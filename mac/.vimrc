set nu "Turn on row numbers
syntax on "Turn on syntax highlighting
colorscheme desert

set smartindent
set tabstop=4
set shiftwidth=4
set expandtab

inoremap <SHIFT><TAB> <BACKSPACE><BACKSPACE><BACKSPACE><BACKSPACE>

set hlsearch "highlight search
"ignore case when searching, except when using capital letters
set ignorecase
set smartcase

set laststatus=1 "Shows file name in status bar if there are >=2 windows
set lbr "Break lines at a character in a word instead of the last character that fits on a screen
